# frozen_string_literal: true

require "rails_helper"

RSpec.describe <%= class_name %>Query, type: :query do
  subject(:query) { described_class.new }

  describe "#entries" do
    it "returns results" do
      expect(query.entries).to be_an(Array)
    end
  end
end
