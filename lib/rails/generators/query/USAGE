Description:
    Generates a new query-file in app/queries and invokes your template
    engine and test framework generators.

Example:
    `bin/rails generate query reports/weekly`

    creates an sql file and test:
        Mailbox:  app/queries/reports/weekly.sql
        Spec:     spec/queries/reports/weekly_query_spec.rb