# frozen_string_literal: true

<% module_namespacing do -%>
class <%= class_name %>Query < ApplicationQuery
  # Declare bind parameters (safe from SQL injection):
  # bind :user_id
  # bind :status, default: nil

  # Declare template variables (for dynamic SQL):
  # var :order_by, default: "created_at DESC"

  # Declare type casting:
  # cast published_at: :datetime, metadata: :json
end
<% end -%>
